
Applies a radial gradient accross screen space, stretching to cover the camera width/height.

Inputs:
 - Vector4 offset (up, down, left, right)
 - Vector1 Falloff
 - Vector1 Size

----


ScreenSpaceGradient2 - just a different way to achive the same thing